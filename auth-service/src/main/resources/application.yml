server:
  port: 8081

spring:
  datasource:
    url: jdbc:h2:mem:authdb
    driverClassName: org.h2.Driver
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: update
  h2:
    console:
      enabled: true

jwt:
  secret: chatsever-jwt-secret-key-2026-safe-key-uit-phat
  expiration: 7200000
  refresh-expiration: 604800000