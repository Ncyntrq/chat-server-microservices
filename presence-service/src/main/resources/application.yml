server:
  port: 8083

spring:
  application:
    name: presence-service
  rabbitmq:
    host: ${SPRING_RABBITMQ_HOST:localhost}
    port: 5672
    username: guest
    password: guest

# Để debug cho dễ
logging:
  level:
    org.springframework.web: DEBUG